import styled from 'styled-components';

const StyledMessages = styled.div`
    height: 100%;

    .chat__messages {
        position: relative;
        height: 100%;
    }
`

export default StyledMessages;