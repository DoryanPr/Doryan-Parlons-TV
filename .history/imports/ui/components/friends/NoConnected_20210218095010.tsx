import React from 'react';
import StyledStories from '../../elements/chats/StyledStories';

const NoConnected = (props: any): JSX.Element => {
    return (
        <StyledNoConnected>
            STORIES
        </StyledNoConnected>
    )
}

export default NoConnected;