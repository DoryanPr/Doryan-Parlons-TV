import styled, { css } from 'styled-components';

const StyledCards = styled.div`

.Cards {
    display: flex;
    flex-direction: row;
    width: 105px;
    height: 125px;
}
`;

export default StyledCards;